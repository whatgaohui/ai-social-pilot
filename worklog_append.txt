TypeScript Error Fixes (17 errors across 8 files)
====================================================

- src/app/api/ai/cover-generate/route.ts: Changed `zai.images.generate()` to `zai.images.generations.create()`, updated size from '1024x1536' to '768x1344' (valid SDK size), replaced `result.data[0].url`/`b64_json` checks with `result.data[0].base64` per the z-ai-web-dev-sdk API.

- src/components/platform-account-panel.tsx: Fixed 8 errors (lines 1796-1826). Changed `onDisconnect` and `onSync` callbacks from `&&` short-circuit (which returns `undefined` when falsy) to ternary operators with `Promise.resolve()` fallback, ensuring return type is always `Promise<void>`.

- src/components/right-panel/account-collector.tsx: Fixed 3 errors. (1) Line 683 & 1449: Cast `m.platforms` as `readonly string[]` before `.includes()` to allow `Platform` union type argument. (2) Line 1128: Wrapped `stat.value` with `Number()` to satisfy `formatNum(number)` parameter type.

- src/components/right-panel/content-collector.tsx: Line 549: Wrapped `stat.value` with `Number()` in `formatNum()` call to handle `string | number` union type.

- src/components/right-panel/hashtag-recommender.tsx: Line 107: Added `as string[]` cast on `fallback` array (from `Set` + `filter(Boolean)`) to satisfy `setHashtags()` `SetStateAction<string[]>` parameter.

- src/components/right-panel/publishing-assistant.tsx: Line 147: Added `as Platform[]` cast to `["wechat", "xiaohongshu"]` array literal so `.map()` callback parameter `p` is typed as `Platform` instead of `string`, matching `PlatformStatus.platform` property type.

- src/components/right-panel/xiaohongshu-preview.tsx: Line 25: Changed `post as Record<string, unknown>` to `post as unknown as Record<string, unknown>` (double cast) since `ContentPost` and `Record<string, unknown>` are not directly assignable.

- src/components/welcome-onboarding.tsx: Line 93: Changed `persona as Record<string, string>` to `persona as unknown as Record<string, string>` (double cast) since `Persona` and `Record<string, string>` are not directly assignable.

Verification: `tsc --noEmit` passes with 0 src/ errors. `bun run lint` passes with no warnings.
